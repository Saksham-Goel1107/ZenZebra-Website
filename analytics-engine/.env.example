# =========================================================================
# ZENZEBRA ANALYTICS ENGINE - ENVIRONMENT VARIABLES
# =========================================================================
# Copy this file to .env and fill in the actual values.
# These variables are required for the Python backend to communicate with
# Appwrite and to secure the internal API calls.

# -------------------------------------------------------------------------
# 1. APPWRITE CONFIGURATION
# -------------------------------------------------------------------------

# WHY: Tells the SDK where your Appwrite server is hosted.
# HOW: Found in Appwrite Console -> Settings -> Overview.
# DEFAULT: https://cloud.appwrite.io/v1
NEXT_PUBLIC_APPWRITE_ENDPOINT=https://fra.cloud.appwrite.io/v1

# WHY: Identifies which project the engine should access.
# HOW: Found in Appwrite Console -> Settings -> Project ID.
NEXT_PUBLIC_APPWRITE_PROJECT_ID=your_appwrite_project_id_here

# WHY: An API Key from the 'Server SDK' section. It must have scopes for:
#      - 'databases.read', 'databases.write'
#      - 'storage.read', 'storage.write'
#      - 'users.read'
# HOW: Appwrite Console -> Project Settings -> API Keys -> Create API Key.
# IMPORTANT: Never share this key or commit it to version control.
APPWRITE_API_KEY=your_appwrite_api_key_here

# -------------------------------------------------------------------------
# 2. DATABASE & STORAGE IDS
# -------------------------------------------------------------------------

# WHY: The specific database where ZenZebra data is stored.
# HOW: Appwrite Console -> Databases -> Select your Database -> Copy Database ID.
NEXT_PUBLIC_APPWRITE_DATABASE_ID=your_appwrite_database_id_here

# WHY: The collection ID where analysis reports and metadata are saved.
# HOW: Appwrite Console -> Databases -> [Database] -> [Collection] -> Copy Collection ID.
APPWRITE_ANALYTICS_COLLECTION_ID=analytics_results

# WHY: The bucket where input Excel files (stock/sales) are downloaded from.
# HOW: Appwrite Console -> Storage -> Select your Bucket -> Copy Bucket ID.
NEXT_PUBLIC_APPWRITE_BUCKET_ID=your_appwrite_bucket_id_here

# -------------------------------------------------------------------------
# 3. SECURITY
# -------------------------------------------------------------------------

# WHY: A shared secret key used to authenticate requests from the Next.js frontend.
#      Requests missing this key in the 'X-API-Key' header will be blocked (403).
# HOW: Generate a random string (e.g., openssl rand -hex 32).
# NOTE: This must match the 'ANALYTICS_SECRET_KEY' in your Next.js .env file.
ANALYTICS_SECRET_KEY=your_analytics_secret_key_here
